# 翻译工具 · 使用指南

一个能翻译网页 + 全局划词翻译的小工具。**完全免费、不用注册付费 LLM**（用智谱 GLM-4-Flash 免费额度）。

> 桌面端管 LLM 调用 · 浏览器扩展负责网页双语对照 · 任意软件里划词都能翻

---

## 第 1 步 · 安装桌面端（必装）

1. 双击 `翻译工具 Setup 0.2.9.exe`
2. 会弹「**Windows 已保护你的电脑**」—— 因为没买代码签名证书。点：
   - **更多信息** → **仍要运行**
3. 选安装目录 → 下一步下一步装完
4. 桌面出现「翻译工具」快捷方式。打开它，**右下角托盘**会出现一个橙色「译」字图标

> ⚠️ 桌面端必须开着，浏览器扩展才能工作。

---

## 第 2 步 · 申请免费 API Key（5 分钟）

智谱 GLM-4-Flash 是**永久免费**模型（有速率限制但日常够用）。

1. 浏览器打开 https://open.bigmodel.cn/usercenter/proj-mgmt/apikeys
2. 用手机号注册账号
3. 进控制台 → 点「**+ 添加新的 API Key**」
4. 起个名字（比如"翻译工具"），点确定
5. **复制生成的那串 Key**（看起来像 `xxxx.yyyy`）

> 💡 如果你已经买过别的翻译 API（DeepSeek / OpenAI / Claude / Moonshot / 通义 / Gemini 等），也可以直接连自己的——工具支持任意 OpenAI 兼容接口。在「+新增配置」里手动填 base_url + api_key + 模型名即可。

---

## 第 3 步 · 把 Key 填进桌面端

### 加一条「网页翻译用」配置

1. 双击桌面「翻译工具」打开主窗口
2. 左边「**API 配置**」→ 右上角「**+新增配置**」
3. 在「**快捷预设**」里点 `🆓 智谱 GLM-4-Flash · 推荐网页`
4. URL 和模型自动填好，**把刚才复制的 Key 粘到「API Key」那一栏**
5. 点「**测试连接**」→ 应该看到「✓ 连接成功，回复：OK」
6. 点「**保存**」
7. 回列表 → 那条配置点「**网页用**」按钮 → 它会变深色，前面带「网页翻译」标签

### 加一条「划词翻译用」配置（推荐）

GLM-4-Flash 翻段落好，但划词要响应快，加一条快版的：

1. 同样点「+新增配置」
2. 选 `🆓 智谱 GLM-4-FlashX · 推荐划词`
3. 填**同一把 API Key**（智谱所有模型共用一把 Key）
4. 保存 → 点「**划词用**」按钮

现在应该有两条配置，一条标着「网页翻译」，一条标着「划词翻译」。

---

## 第 4 步 · 开启全局划词翻译

1. 左边「**通用设置**」
2. 把「**全局划词翻译**」开关打开
3. 把「**鼠标选中自动翻译**」也打开（不用按快捷键，选中就翻）
4. **划词快捷键**默认 `Alt+Z`（冲突可以改，预设有 Alt+Q / Ctrl+Alt+Z 等）
5. **OCR 截图快捷键**默认 `Alt+X` —— 按一下自动唤起 Win+Shift+S 截图，截完自动 OCR + 翻译

到这里桌面端配置完了。**在任何软件里**（Word / PDF / 聊天窗口 / 记事本）选中英文，会自动弹翻译窗；不能选中的（PDF 扫描件、视频字幕、图片）按 `Alt+X` 截图也能翻。

---

## 第 5 步 · 装浏览器扩展（用于网页整页双语对照）

桌面端只能划词。读整篇英文需要扩展来做「英文 + 中文上下行对照」。

1. 找到 `extension-0.2.9.zip` 文件
2. **解压到任意目录**（⚠️ 别删了，扩展靠这个目录跑，比如解压到 `D:\翻译工具扩展\`）
3. Chrome / Edge 浏览器地址栏输入 `chrome://extensions/`
4. 右上角打开「**开发者模式**」
5. 点「**加载已解压的扩展程序**」
6. 选刚才解压出来的那个目录
7. 浏览器工具栏出现橙色「译」字图标

---

## 第 6 步 · 用起来

- **任意软件划词** → 鼠标拖选英文 → 自动弹翻译窗
- **任意软件按 `Alt+Z`** → 翻译选中的文字
- **任意软件按 `Alt+X`** → 自动唤起 Win+Shift+S 截图 → OCR 识别 + 翻译（适合 PDF 扫描件、视频字幕、不能选中的文字）
- **浏览器读英文网页** → 自动在段落下方插入中文译文
- **翻译弹窗点「加入生词本」** → 词条进生词本，到期会弹系统通知提醒复习

### 主窗口左侧 sidebar 4 个 tab

| Tab | 干嘛的 |
|---|---|
| **API 配置** | 管理 LLM Key（第 3 步已配过） |
| **通用设置** | 划词/OCR 快捷键、源/目标语言、翻译风格 |
| **生词本** | 划词弹窗里点「加入」的词条都在这。SRS 间隔复习（5 分钟 → 30 分钟 → 1 小时 → 6 小时 → 12 小时）自动弹通知 |
| **术语表** | 录入「源词 → 目标词」对照（人名 / 品牌 / 自定义术语），翻译时自动注入到 prompt 让 LLM 遵守 |

### 翻译风格预设

通用设置 → **翻译风格**，3 种：
- **通用** —— 准确平实，适合大多数内容
- **小说 · 文学** —— 偏文学表达，适合 AO3、小说网文
- **技术文档** —— 严谨直译，适合 API 文档、技术博客

> 只影响**浏览器扩展的整页翻译**。划词为了响应速度始终用极简 prompt，不受这里影响；API 配置里如果填了自定义 system_prompt，那条优先级最高。

### 浏览器扩展开关说明

点扩展图标打开 popup，从上往下依次是：

| 开关 | 作用域 | 是否被记住 |
|---|---|---|
| **扩展启用**（master） | 全局总开关 · 关掉所有网站都不翻 | ✅ 持久 |
| **启用翻译** | 仅当前这次访问 | ❌ 关浏览器就忘 |
| **双语对照** | 显示模式：开 = 原文+译文 / 关 = 仅显示译文 | ✅ 持久 |
| **默认翻译此网站** | 此网站持久默认 · 决定下次访问要不要自动翻 | ✅ 持久 |

举例：
- 想读 AO3 / 文档站：默认翻译此网站 ON，每次打开都自动翻
- 不想读 google.com 翻译：默认翻译此网站 OFF
- 偶尔想翻一次某个不在记住名单的网站：临时点「启用翻译」ON

---

## 常见问题

### Q: 桌面端打开后窗口不见了？
A: 躲到右下角托盘里了。找橙色「译」字图标，**双击或右键 → 打开设置**。

### Q: 选了文字按 Alt+Z 没反应？
1. 桌面端「通用设置」→「全局划词翻译」开关有没有开
2. 托盘图标右键看「✓ 划词翻译已开启」对不对
3. Alt+Z 跟别的软件（QQ / 网易云 / 笔记）冲突 → 设置里换成 `Alt+Q` 或 `Ctrl+Alt+Z`（**别用 Alt+X，那是 OCR 截图的快捷键**）

### Q: 浏览器扩展显示「未连接」？
A: 桌面端没在跑。**先打开桌面端**，扩展会自动连上。

### Q: 网页翻译特别慢 / 全是 [翻译失败]？
A: 大概率是 API Key 没配好或者免费额度用完了。
- 打开桌面端 →「API 配置」→ 找网页用那条 → 点「测试连接」看具体报错
- 看到 `429 / 1113 / 余额不足` 就是模型额度用满了，换一个模型（比如把 FlashX 换成不带 X 的 Flash，额度独立）
- 看到「未连接」就是 Key 错了或网络问题

### Q: 浏览器扩展点「重试翻译」也没反应？
A: 关掉扩展 popup 上的「扩展启用」master 开关再开一次，强制重新连接。

### Q: 翻译质量不满意？
A: 桌面端 API 配置加一条「智谱 GLM-Z1-Flash · 推理型」预设 → 慢一倍但更聪明，适合小说、文学性长段落。

### Q: 怎么完全删掉？
A: 控制面板「卸载程序」找「翻译工具」卸载。要清干净配置：删 `%APPDATA%\desktop` 这个文件夹（资源管理器地址栏粘 `%APPDATA%` 回车找；里面有 `data.db` 存了你的 Key 和生词本）。
